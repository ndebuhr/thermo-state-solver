#!/usr/bin/env python

from docopt import docopt

import state_eqns_gen
import state_points_gen

if __name__ == '__main__':
    # parse the command-line options
    options = docopt(__doc__, version='thermostate 0.0.1')

